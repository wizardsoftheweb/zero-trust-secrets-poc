apiVersion: v1
kind: Service
metadata:
  name: {{ include "zts-poc.fullname" . }}
  labels:
{{ include "zts-poc.labels" . | indent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "zts-poc.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
